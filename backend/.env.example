# Copy to backend/.env and adjust.
# Docker Compose overrides DATABASE_URL when using compose.dev.yaml / compose.prod.yaml.

# Local Postgres (outside Docker): use localhost and match your Postgres user/database.
# DATABASE_URL=postgresql://USER:PASSWORD@localhost:5432/DATABASE
